{
  "name": "booking",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "workspaces": [
    "packages/*",
    "services/*"
  ],
  "engines": {
    "node": ">=24 <25",
    "npm": ">=11 <12"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "test": "vitest run",
    "typecheck": "tsc -b",
    "workspace:check": "node tools/check-workspace.mjs",
    "compliance:check": "node tools/check-compliance.mjs",
    "evidence:generate": "node tools/generate-evidence.mjs",
    "audit:deps": "npm audit --audit-level=high",
    "check": "npm run workspace:check && npm run compliance:check && npm run test && npm run build",
    "ci": "npm run check && npm run audit:deps",
    "preview": "vite preview"
  },
  "dependencies": {
    "lucide-react": "^1.44.0",
    "react": "^19.3.0",
    "react-dom": "^19.3.0"
  },
  "devDependencies": {
    "@types/react": "^19.3.0",
    "@types/react-dom": "^19.3.0",
    "@vitejs/plugin-react": "^6.1.1",
    "typescript": "^7.0.2",
    "vite": "^8.2.0",
    "vitest": "^5.0.0"
  }
}
